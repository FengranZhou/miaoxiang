# 妙想工作台 · 使用须知（本块由妙想更新器自动维护，请勿手改）

## 按图生（按参考图风格生成新图）

- 生图只有一条路：走「按图生」skill 的完整闭环（双 tab 生成 → 挑 winner → 评审 → 终稿确认浮窗）。目录里**没有**任何「一条命令直接出图」的捷径脚本，不要凭想象假设有。
- 跑底层脚本前先 `ls ~/.claude/skills/按图生/` 或读 SKILL.md 确认入口脚本名（是 `doubao-gen.mjs`），禁止凭记忆拼脚本名。
- 写生图 prompt 之前必读 SKILL.md 第 2 步（按主题词视觉歧义度决定详简 + 写 prompt 硬纪律）。
- 首次使用会自动弹出一个专用 Chrome 窗口（调试端口 9333），用自己的豆包账号登录一次即可，之后复用登录态。
- 图像后处理（抠图 / 去水印 / PIL）一律用 `~/.claude/skills/按图生/.venv/bin/python3`——macOS 系统 python 受保护装不了第三方包。
- 抠图：`~/.claude/skills/按图生/.venv/bin/python3 ~/.claude/skills/按图生/cutout.py <输入> <输出>`；本机装了 Eagle +「AI 自动抠图」插件会被自动探测并提速数倍。

## 去水印

- 豆包生成图右下角的「豆包AI生成」水印，用「去水印」skill 处理（说"去水印"+给图片路径即可触发）。

## 效率原则

- 涉及「按图生」这类耗时生成时，生成命令用后台方式跑，等待的约 90 秒里并行去改 HTML/CSS（img src 先用交付落点占位），不要干等。
