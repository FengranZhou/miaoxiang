# 按图生 V5.0 全自动自迭代 + AI 审美闭环 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 把 V4.0.4.4 的「picker 浮窗 + 三轮断点汇报」全部去掉，改成主流程 AI 宏观挑 winner + 评审 skill 加形态审美评判 + 收敛性兜底，实现全自动自迭代直到产出高质量风格一致表意清晰的图。

**Architecture:**
- **按图生 skill**：双 tab 生成完后，主流程在同回合内并行 Read 3 张图（参考图 + A + B），用宏观综合 prompt 挑 winner（输出两张观察 + winner + 一句话原因），不再唤起 SwiftUI picker。三轮汇报改为收敛性检测（连续 3 轮结论 + 关键差异前三高度重复才汇报）。
- **按图生-评审 skill**：在 Step C+（语义合理性扫描）之后、Step D（细节差异识别）之前，新增 Step C++ 形态审美评判。不参照参考图，仅靠主题词领域先验对 winner 主体形态独立打分；不合格直接覆盖为「重生成」结论。
- **doubao-gen.mjs**：移除 `invokeUserPicker()` 函数调用 + 简化 handoff（`next_action` 永远是 `main_pick_winner` 或 `await_user`，移除 `invoke_review` / `user_picked_neither`）。picker 二进制保留不删（回滚预案）。

**Tech Stack:**
- 按图生 / 按图生-评审：Markdown skill 文档（Claude 读后按指令执行）
- doubao-gen.mjs：Node.js + Playwright CDP（豆包图像生成自动化）
- bin/picker：SwiftUI 二进制（V5 不再调用，保留代码以便回滚）

**仓库**（两个独立 repo）:
- 按图生：`git@github-personal:FengranZhou/claude-skill-antusheng.git`（路径 `~/.claude/skills/按图生`）
- 按图生-评审：`git@github-personal:FengranZhou/claude-skill-antusheng-pingshen.git`（路径 `~/.claude/skills/按图生-评审`）

---

## Task 0：双 repo 打安全 tag

**Files:**
- 无文件改动；纯 git 操作

**Step 1：在按图生 repo 打 tag `pre-v5-2026-06-18`**

```bash
cd /Users/iflytek/.claude/skills/按图生
git status   # 必须 clean
git tag pre-v5-2026-06-18
git push github-personal pre-v5-2026-06-18
```

Expected: tag 推送成功，可在 GitHub 上看到 `pre-v5-2026-06-18`

**Step 2：在按图生-评审 repo 打 tag `pre-v5-2026-06-18`**

```bash
cd /Users/iflytek/.claude/skills/按图生-评审
git status   # 必须 clean
git tag pre-v5-2026-06-18
git push github-personal pre-v5-2026-06-18
```

Expected: tag 推送成功

**Step 3：验证两 repo 都已打 tag**

```bash
cd /Users/iflytek/.claude/skills/按图生 && git tag --list | grep pre-v5
cd /Users/iflytek/.claude/skills/按图生-评审 && git tag --list | grep pre-v5
```

Expected: 两 repo 都输出 `pre-v5-2026-06-18`

---

## Task 1：doubao-gen.mjs 简化 handoff（移除 picker 主路径）

**Files:**
- Modify: `/Users/iflytek/.claude/skills/按图生/doubao-gen.mjs`（主要是 685-878 行的 Handoff 部分）

**Step 1：定位需修改的代码块**

打开 `/Users/iflytek/.claude/skills/按图生/doubao-gen.mjs`，确认以下结构：

- L689-878：handoff JSON 构造 + AUTO_REVIEW 分支
- L708-783：`invokeUserPicker()` 函数定义
- L785-878：`if (AUTO_REVIEW) { ... }` 主分支

**Step 2：用以下完整替换 L689-878 段落（直至 file 末尾）**

```javascript
// === Handoff JSON ===
// V5.0：不再调用 picker 浮窗。
//   - 双 tab 成功生成 2 张 → main_pick_winner（主流程并行 Read 3 张图 + 宏观挑 winner）
//   - 双 tab 只成功 1 张 → main_pick_winner（主流程直接把这张当 winner，无需挑）
//   - 单 tab + auto-review → main_pick_winner（同上）
//   - 没有 auto-review → await_user
const candidates = results.map(r => ({
  tag: r.tag,
  complexity: r.complexity,
  path: r.path,
}));

let nextAction = "await_user";
let pickPayload = null;

if (AUTO_REVIEW) {
  nextAction = "main_pick_winner";
  pickPayload = {
    ref_path: REF,
    orig_ref_path: ORIG_REF,
    candidates,
    theme: THEME,
    round: ROUND,
    temp_dir: TEMP_DIR,
    next_skill: candidates.length >= 2 ? "按图生（主流程宏观挑 winner，再调按图生-评审）" : "按图生-评审（仅 1 张直接送评审）",
    note: candidates.length >= 2
      ? "V5.0 主流程自主挑 winner：同回合内并行 Read 3 张图（orig_ref + A + B），按「风格贴合 + 主体形态 + 主题贴合 + 整体观感」宏观综合评判，强制输出两张观察 + winner + 一句话原因。挑完 rm 落选 + mv winner → 第N轮-winner.png，再 invoke 按图生-评审。"
      : "仅 1 张候选，直接当 winner 送评审。",
  };
}

// === Output ===
const handoff = {
  status: "generated",
  mode: MODE,
  candidates,
  errors,
  temp_dir: TEMP_DIR,
  task_id: TASK_ID,
  round: ROUND,
  next_action: nextAction,
  pick_payload: pickPayload,
  original_ref_path: ORIG_REF,
};

console.log("\n" + JSON.stringify(handoff, null, 2));

if (AUTO_REVIEW) {
  if (nextAction === "main_pick_winner") {
    console.log("\nMAIN_PICK_WINNER: 主流程接管挑 winner（V5.0 全自动路径）");
    console.log(`  candidates=${candidates.map(c => c.path).join(",")}`);
    console.log(`  orig_ref=${ORIG_REF}`);
    console.log(`  ref=${REF}`);
    console.log(`  temp_dir=${TEMP_DIR}`);
    console.log(`  theme=${THEME}`);
    console.log(`  round=${ROUND}`);
  } else if (nextAction === "await_user") {
    console.log("\nAWAIT_USER: --auto-review 未启用");
  }
}
```

**Step 3：删除以下已废弃代码段**

在 Step 2 替换之后，文件应当只剩到上面 `}` 收尾。确认以下内容**已被完全删除**：

- `function noPickerSentinelPath()` 函数（原 L717-720）
- `async function invokeUserPicker()` 函数（原 L722-783）
- `let userPick = null` / `let userComplaint = null` / `let winnerPath = null` 三个变量
- `reviewPayload` 变量及其所有赋值
- 所有 `invoke_review` / `user_picked_neither` 分支
- 所有 `import("node:child_process")` 用于 picker 的 dynamic import

**Step 4：注释头部说明同步更新（L1-22）**

把文件头部的说明注释（L4-22 那段）更新为：

```javascript
// 单 tab 模式（兼容 v3.4）：
//   node doubao-gen.mjs --ref <image> --prompt "<text>" --theme "<text>" [--round N] [--auto-review]
//
// 双 tab 并行模式（V5.0 全自动）：
//   node doubao-gen.mjs --ref <image> --prompt-a "<极简>" --prompt-b "<标准>" --theme "<text>" [--round N] [--orig-ref <image>] [--auto-review]
//     --prompt-a   极简档 prompt（只主体描述+输出）
//     --prompt-b   标准档 prompt（v3.4 完整 9 字段）
//     --orig-ref   原始参考图（首轮用户给的那张）。迭代轮次里 --ref 通常是"上轮 winner"。
//                  V5.0 主流程挑 winner 时左/对照位用 ORIG_REF（保持原始参考图认知锚点稳定）
//
// V5.0 变更：移除 picker 浮窗，主流程接管挑 winner。
//   --auto-review        生成完后输出 next_action = main_pick_winner
//                        主流程并行 Read 3 张图（orig_ref + A + B）按宏观维度自主挑 winner
//                        强制输出：两张观察 + winner + 一句话原因
//                        挑完 rm 落选 + mv winner → 第N轮-winner.png，再 invoke 按图生-评审
//   --theme "<text>"     主题词（必填于 auto-review 时）
//   --round N            当前轮次（默认 1）
//   生成原图存到 /tmp/按图生-评审/{任务ID}/
```

**Step 5：语法 + 干跑验证**

```bash
cd /Users/iflytek/.claude/skills/按图生
node --check doubao-gen.mjs
node doubao-gen.mjs   # 不带参数，应该报 Usage 错误并 exit 1
```

Expected:
- `node --check` 无任何输出（语法 OK）
- 直接跑应输出 `Usage: ...` 然后 exit code 1

**Step 6：Commit doubao-gen.mjs**

```bash
cd /Users/iflytek/.claude/skills/按图生
git add doubao-gen.mjs
git commit -m "$(cat <<'EOF'
V5.0(WIP): doubao-gen.mjs 移除 picker 主路径，handoff 永远 main_pick_winner

- 移除 invokeUserPicker() 函数 + sentinel 机制
- handoff next_action 简化为 main_pick_winner / await_user
- pick_payload 改为 candidates + orig_ref_path + ref_path，供主流程并行 Read 3 张图挑 winner
- 移除 user_pick / user_complaint / invoke_review / user_picked_neither 分支
- bin/picker 二进制保留不删（回滚预案）

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: commit 成功，git status clean

---

## Task 2：按图生 SKILL.md 改写 Step 3.7（主流程 AI 挑 winner）

**Files:**
- Modify: `/Users/iflytek/.claude/skills/按图生/SKILL.md`（Step 3.5、Step 3.7 段落 + 相关引用）

**Step 1：替换 Step 3.5 的 next_action 路由表**

打开 `/Users/iflytek/.claude/skills/按图生/SKILL.md`，找到"### Step 3.5: 解析脚本输出"段落（约 L162-215）。

把那张 `next_action` 路由表替换为：

```markdown
**根据 `next_action` 路由**：

| `next_action` | 含义 | 主流程动作 |
|---|---|---|
| `main_pick_winner` | V5.0 唯一主路径（双图或单图都走这条） | 进入 **Step 3.7（主流程宏观挑 winner）** |
| `await_user` | 没带 `--auto-review`，按用户要求处理 | — |

**V5.0 变更**：脚本不再唤起 picker 浮窗，handoff 总是返回 `main_pick_winner`。主流程自主完成挑 winner 动作（同回合并行 Read 3 张图 + 宏观综合评判）。

**禁止**输出"生成完成请告诉我是否要评审/挑图"之类等待提示，**禁止**自行评审，**禁止**自行打分。**唯一允许的下一动作**：进入 Step 3.7。
```

**Step 2：完整替换 Step 3.7 段落**

找到"## Step 3.7: 用户 picker 挑 winner（V4.1 主路径）"段落（约 L217-250），完整替换为：

```markdown
## Step 3.7: 主流程宏观挑 winner（V5.0 全自动主路径）

V5.0 不再有用户 picker 浮窗，主流程接管挑 winner。

### 单图情况（candidates 只有 1 张）

直接把这张当 winner：

```bash
mv <唯一候选路径> <temp_dir>/第N轮-winner.png
```

然后进入 Step 4 评审。

### 双图情况（candidates = A + B，主流程核心）

#### Step 3.7.1: 同回合内并行 Read 3 张图（强制）

```
Read(pick_payload.orig_ref_path)
Read(candidates[0].path)   # A 极简
Read(candidates[1].path)   # B 标准
```

**铁律**：必须在同一回合内并行发出 3 个 Read，禁止跳过、禁止 sequential。

#### Step 3.7.2: 宏观综合评判（强制按下面这个 prompt 自我对话）

读完 3 张图，主流程必须按下面这个**结构化文本**输出（不需要 JSON），评判维度是**宏观综合**——不局限在形态：

```
【候选 A 一句话观察】<对 A 的客观描述，覆盖风格贴合度 + 主体形态造型力 + 主题贴合度 + 整体观感，30-80 字>
【候选 B 一句话观察】<同上，对 B>
【Winner】A 或 B
【一句话原因】<为什么选这张，要具体到差异点，不能写"A 更好"这种空话>
```

**铁律**：

1. **必须先输出 A、B 两段观察，再输出 winner + 原因**——禁止跳过观察直接给结论
2. **评判维度是宏观综合**（风格 + 形态 + 主题 + 整体观感），**不要只看形态、不要只看风格**——任一单维度都不足以判好坏
3. **一句话原因必须落到具体差异点**：
   - ❌ "A 整体更好" / "B 更符合参考图" / "A 更精致"（虚）
   - ✅ "A 的颈肩过渡比 B 自然，B 的肩部是直角梯形显得生硬" / "A 配色更接近参考图的低饱和哑光，B 偏鲜艳"（实）
4. **即使两张都不好也必须选一个**——下游评审会兜底重生成，不要在这里纠结
5. **不要参照"应该长什么样"的预设**——只看眼前 3 张图，用主题词（pick_payload.theme）作为评判锚点

#### Step 3.7.3: 执行挑选结果（机械动作）

```bash
WINNER_TAG=<A 或 B>
LOSER_TAG=<另一个>
WINNER_PATH=<对应 candidates 里那个 tag 的 path>
LOSER_PATH=<另一个>
TEMP_DIR=<pick_payload.temp_dir>
ROUND=<pick_payload.round>

rm "$LOSER_PATH"
mv "$WINNER_PATH" "$TEMP_DIR/第${ROUND}轮-winner.png"
```

#### Step 3.7.4: 进入 Step 4 评审

把 `第N轮-winner.png` 路径作为评审的 `image_paths[0]` 送入按图生-评审 skill。

### V5.0 核心铁律

- **3 张图必须 Read，禁止凭空判**——回合内并行 3 个 Read，少一个都算违规
- **输出顺序必须是「两段观察 → winner → 原因」**——倒序或跳步都算违规
- **评判维度是宏观综合**——不局限形态、不局限风格、不局限主题，全部一起看
- **picker 浮窗 + user_complaint 机制已废弃**——用户主动调用时如想给吐槽，可在调用 prompt 里直接说，不走自动路径
- **没有「都不好」分支**——必须从 A/B 选一个，评审会兜底重生成
```

**Step 3：删除「都不好」专属纪律段落**

找到"#### 「都不好」专属纪律（V4.2 — `next_action: "user_picked_neither"` 入口）"段落（约 L398-414），**完全删除**——V5.0 没有这条路径了。

**Step 4：替换 Step 4「断点汇报」为「收敛性检测」**

找到"## Step 4: 验收（外置评审 skill）"段落里的"唯一例外"小节（约 L288-291），把这段：

```markdown
**唯一例外**（需要用户介入）：
- **断点汇报**：在第 3、6、9... 轮（每 3 轮的整数倍）迭代仍未 通过 → 停下来向用户汇报状态。**仅在断点轮汇报**，断点之间所有轮次照常自动迭代，不打扰用户
- 用户显式去掉 `--auto-review` → 走人工评审路径
```

替换为：

```markdown
**唯一例外**（需要用户介入）：
- **收敛性兜底**：每轮评审结束后，主流程把 `(round, 结论, 关键差异前三)` 追加到 `<temp_dir>/iteration-log.json`。
  当文件里**连续 3 轮**满足以下条件时 → 停下来向用户汇报「陷入相同问题循环」，建议手动介入：
    a. 评审结论完全相同（都是「重生成」或都是「修订」）
    b. 且每轮的「关键差异前三」之间相似度 ≥ 80%（3 条差异里至少 2 条语义相同/高度重叠）
  否则**一直自迭代不打扰用户**——不限轮数，不每 N 轮汇报。
- 用户显式去掉 `--auto-review` → 走人工评审路径

**收敛性检测实现**（伪代码，在每次评审结束、进入 Step 4.1 之前执行）：

```javascript
// 每轮评审完后追加
const log = JSON.parse(fs.readFileSync(`${temp_dir}/iteration-log.json`, "utf8") || "[]");
log.push({ round, 结论, 关键差异前三 });
fs.writeFileSync(`${temp_dir}/iteration-log.json`, JSON.stringify(log, null, 2));

// 第 4 轮起做检测
if (log.length >= 3) {
  const last3 = log.slice(-3);
  const sameConclusion = last3.every(x => x.结论 === last3[0].结论) && last3[0].结论 !== "通过";
  if (sameConclusion) {
    // 判定关键差异前三相似度：每两轮之间，3 条差异里至少有 2 条语义重叠 = 高度相似
    const r1 = similar(last3[0].关键差异前三, last3[1].关键差异前三);
    const r2 = similar(last3[1].关键差异前三, last3[2].关键差异前三);
    if (r1 && r2) {
      // 死循环汇报：停下来告诉用户
      报告({
        消息: "连续 3 轮陷入相同问题循环",
        最近3轮: last3,
        建议: "手动介入：换参考图 / 换主题描述 / 手动调 prompt"
      });
      return;  // 不再自迭代
    }
  }
}
// 否则继续自迭代
```

主流程不需要写代码做相似度判断——可以由 LLM 直接读 last3 数据后用语义判断。重点是**强制每轮追加 log 文件 + 每轮做检测**这两个动作。
```

**Step 5：更新顶部版本说明**

找到文件头部（L1-30 左右），把所有 `V4.1` / `V4.0.4.x` 提及更新为 `V5.0`：

```yaml
---
name: 按图生
description: 按参考图风格生成新主题图片（通过豆包图像生成）
version: 5.0.0
backend: doubao
mode: dual-tab-fully-auto
---

# 按图生 V5.0 — 双 tab 并行 + 主流程宏观挑 winner + 评审形态审美闭环

基于参考图风格生成新主题的图片。底层使用**豆包图像生成**，通过 Playwright CDP 自动化。

**V5.0 关键变更**：
- **去 picker**：双 tab 双图生成完后，主流程**同回合并行 Read 3 张图 + 宏观综合评判挑 winner**，不再唤起 SwiftUI 浮窗。判定输出强制「两张观察 + winner + 一句话原因」。
- **加形态审美**：评审 skill 在 Step C+ 之后新增 Step C++ 形态审美评判（仅靠主题词锚 + 不参照参考图），不合格直接覆盖为「重生成」结论。
- **去三轮断点汇报**：改成收敛性兜底——连续 3 轮评审结论相同 + 关键差异前三高度重复才汇报，否则一直自迭代不打扰用户。

**V4.0 双 tab 并行生成基础保留**：每轮在 2 个独立 chrome tab 中同时跑 2 份不同复杂度的 prompt。评审 skill 的契约**完全不变**（始终是单图评审）。

**V3.4 之前的"中文 JSON 全流程"基础设施保留**：所有 prompt 相关流程（反推、重写、优化、输出、迭代）仍然使用中文 JSON 格式，仍然强制 `jq -c .` 序列化。

**核心能力**：参考图 + 主题词 → 2 张并行生成图 → **主流程 AI 自主挑 winner** → 评审（含形态评判）闭环 → 自迭代直到通过。
```

**Step 6：更新文件底部「核心原则」段落**

找到"## 核心原则（V4.1 Always）"段落（约 L452-462），更新为：

```markdown
## 核心原则（V5.0 Always）

1. **参考图驱动**：风格描述从参考图提取，不凭空想象
2. **双 tab 永远并行**：首轮 + 所有迭代轮，始终 A + B 双档
3. **A 档纯自然语言**：一句话大白话（首轮 2-6 字主题词；迭代时可加 1-3 词指向评审最严重 1 个问题，如"淡粉色的文件夹"）；绝不堆砌、绝不结构化、不写风格词、不用 JSON
4. **B 档完整中文 JSON**：沿用 v3.4 全流程，9 字段 + 装饰元素决策 + 排除清单
5. **winner 由主流程自主挑**：同回合并行 Read 3 张图（orig_ref + A + B），按宏观综合维度判，强制输出"两张观察 + winner + 一句话原因"
6. **评审权威 + 形态评判兜底**：评审 skill 是质量守门员，结论权威；形态评判加在 Step C+ 之后做独立硬关卡，不合格直接重生成
7. **自迭代不打扰**：评审未通过自动迭代，连续 3 轮陷入相同问题循环才汇报，否则一直跑
8. **B 档中文 JSON UTF-8 不可丢**：序列化必须用 `jq -c .` 或 `json.dumps(ensure_ascii=False)`
9. **Prompt 迭代 > 重拍参考图**：参考图稳定不变，调 prompt
10. **按需加载文档**：用到详细规则时再 Read 对应文档
```

**Step 7：更新「异常情况处理」表**

找到 `## 异常情况处理` 表（约 L437-448），删除所有 picker 相关行（picker 浮窗未显示、用户长时间没点、用户选了「别烦我」、picker 浮窗启动后用户切走没点），保留剩余无关行。

**Step 8：Commit SKILL.md**

```bash
cd /Users/iflytek/.claude/skills/按图生
git add SKILL.md
git commit -m "$(cat <<'EOF'
V5.0(WIP): SKILL.md 改写主流程挑 winner + 收敛性兜底

- Step 3.7：picker 浮窗 → 主流程同回合并行 Read 3 张图 + 宏观综合挑 winner
- Step 4：三轮断点汇报 → 收敛性检测（连续 3 轮相同结论 + 关键差异前三 80% 相似才汇报）
- 删除「都不好」专属纪律段落
- 删除 picker 相关异常处理行
- 顶部版本说明 + 核心原则全部升到 V5.0

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: commit 成功

---

## Task 3：评审 SKILL.md 新增 Step C++ 形态审美评判

**Files:**
- Modify: `/Users/iflytek/.claude/skills/按图生-评审/SKILL.md`

**Step 1：在出参 JSON schema 加 `形态评判` 字段**

打开 `/Users/iflytek/.claude/skills/按图生-评审/SKILL.md`，找到「### 出参」JSON 示例（约 L37-126），在 `"语义扫描"` 字段之后、`"细节差异"` 字段之前，插入：

```json
  "形态评判": {
    "部件枚举": ["string (列出主体上所有可命名的部件)"],
    "形态描述": "string (一句话客观描述主体形态)",
    "评级": "合格 | 不合格",
    "原因": "string (一句话说明评级理由)"
  },
```

**Step 2：在「## 评审流程」中新增 Step C++ 段落**

找到 `### Step C+：检查点 1.5 — 语义合理性扫描` 段落（约 L241-289），在它**之后**、`### Step D：检查点 2 — 细节差异识别` **之前**，插入新段落：

```markdown
### Step C++：检查点 1.7 — 形态审美评判（V5.0 新增）

**仅当 Step C 全字段一致 且 Step C+ 语义扫描通过时执行**。否则 `形态评判.评级: "不适用"`。

**目的**：补齐 V4.x 评审盲区——9 字段全通过 + 装饰物语义合格的图，仍可能因为主体形态「圆+梯形拼接」这类造型偷懒问题而审美不合格。本 Step 独立判主体形态造型力。

**关键纪律**：

1. **不参照参考图**——纯靠主题词领域先验做判断（参考图只给「风格」锚点，给不出「造型力下限」锚点）
2. **仅评主体形态**——不评风格、不评配色、不评质感（这些已在 Step C / C+ 评过）
3. **必须先 enumerate 后评级**——先逐项列出主体部件，再给评级，禁止直接打分
4. **必须给具体原因**——评级理由不能是"看着不行"，必须落到部件、转折、剪影上

**评判 prompt（自我对话用）**：

```
仅从形态的角度评判 winner，主题是 <theme>，不考虑其他要素，仅评判主体形态本身。

输出：
- 主体部件枚举：[列出主体上所有可命名的部件]
- 形态描述：[一句话客观描述]
- 评级：合格 | 不合格
- 一句话原因：[为什么合格/不合格]

判定参考：
- 部件丰富度（仅 2 个基础几何元拼成的极简形 → 倾向不合格；多个部件且有转折过渡 → 倾向合格）
- 主体是否具备"经过造型加工"的感觉（vs"占位符级别"）
- 必须结合主题词的领域先验：
  - avatar/头像/人像 → 期望解剖结构（头/颈/肩/耳/面部特征），裸圆+梯形 = 不合格
  - 标准图标/icon → 可以极简，但应有清晰几何意图（线性 / 填充 / 角标），毛糙拼接 = 不合格
  - 装饰类（贴纸/插画/角色）→ 期望造型语言完整（剪影丰富、有节奏感）
```

**写入示例**（蓝绿头像案例）：

```json
"形态评判": {
  "部件枚举": ["头部", "颈部", "肩部", "耳朵（简化）"],
  "形态描述": "椭球头部 + 有机曲线颈肩过渡，剪影呈葫芦形",
  "评级": "合格",
  "原因": "部件完整且有可读的人体结构，颈肩过渡自然"
}
```

```json
"形态评判": {
  "部件枚举": ["头部（正圆）", "肩部（梯形）"],
  "形态描述": "正圆+直角梯形拼接，头颈断裂",
  "评级": "不合格",
  "原因": "主体由两个基础几何元直接拼接，无颈部过渡、无耳朵、肩部为生硬几何切片，造型停留在占位符级别"
}
```

**判定后果**：

| 评级 | 后果 |
|---|---|
| `合格` | 进入 Step D 继续走原本细节差异评判 |
| `不合格` | **结论 = `重生成`**（覆盖 Step D 的判定），策略 A；触发 Step 4.1 重生成路径 |

**铁律**：
- **形态不合格 = 重生成，不是修订**——形态平庸是 prompt 「主体描述不够具象」的源头问题，渐进式微调改不了
- **不合格时，把 `形态描述` + `原因` 写进 `结论详情.关键差异前三` 第一条**，便于下游 Step 4.1 重生成时把"形态应该是什么样"作为最优先信号
```

**Step 3：更新「## 评审流程」总览段（如有）**

如果文件里有"评审流程"的概览（A → A+ → B → C → C+ → D → D+ → E），更新为 A → A+ → B → C → C+ → **C++** → D → D+ → E。

**Step 4：更新顶部版本说明**

把头部的 `V4.1` 升到 `V5.0`：

```markdown
# 按图生-评审 V5.0（中文 JSON 全流程 + 形态审美评判）
```

并在"设计变更说明"段落追加 V4 → V5 的说明：

```markdown
> **设计变更说明（V4.0 → V4.1）**：所有评审输出从英文字段切换为中文字段。...（原有内容保留）
>
> **设计变更说明（V4.1 → V5.0）**：评审流程新增 Step C++ 形态审美评判，作为 Step C+ 之后、Step D 之前的独立硬关卡。补齐 V4.x 盲区——9 字段全通过 + 装饰物语义合格的图，仍可能因为主体形态「圆+梯形拼接」这类造型偷懒而审美不合格。形态评判不参照参考图（参考图只锚风格、不锚造型力），仅靠主题词领域先验做判断，不合格直接覆盖为「重生成」结论。同时 V5.0 上游主流程已废弃 picker 浮窗，winner 由按图生主流程自主挑选；本 skill 入参 `user_complaint` 退化为可选历史字段（向后兼容，默认链路不再触发 Step D+）。
```

**Step 5：Commit 评审 SKILL.md**

```bash
cd /Users/iflytek/.claude/skills/按图生-评审
git add SKILL.md
git commit -m "$(cat <<'EOF'
V5.0(WIP): 评审新增 Step C++ 形态审美评判

- 加在 Step C+ 之后、Step D 之前的独立硬关卡
- 仅看 winner 一张，不参照参考图（参考图只锚风格、不锚造型力）
- 必须 enumerate 部件 + 主题词领域先验 + 一句话原因
- 评级=不合格 → 覆盖为「重生成」结论
- 出参 JSON 新增 形态评判 字段（部件枚举/形态描述/评级/原因）
- 顶部版本升至 V5.0

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: commit 成功

---

## Task 4：评审 REVIEW-DISCIPLINE.md 加形态评判铁律

**Files:**
- Modify: `/Users/iflytek/.claude/skills/按图生-评审/REVIEW-DISCIPLINE.md`

**Step 1：在文件末尾追加铁律 5**

```markdown

---

## 铁律 5：Step C++ 形态评判禁参照参考图（V5.0 新增）

**触发条件**：执行 Step C++ 形态审美评判时。

**强制行动**：
- **只看 winner（生成图）一张**，禁止再回看参考图
- 评判维度**只有主体形态造型力**（部件丰富度 / 转折过渡 / 剪影完整度），禁止评风格 / 配色 / 质感（这些已在 Step C / C+ 评过）
- 评判锚点**只有主题词的领域先验**——靠你训练时见过的 dribbble / iconfont / figma 上"合格作品"的造型水准做判

**禁止**：
- "参考图就是圆+梯形，所以生成图也是圆+梯形也合格" ← 参考图给的是风格锚点不是造型锚点；这逻辑会让评审退化
- "感觉差不多" / "看着还行" ← 印象不是 enumerate；必须先列部件再评级
- "不知道该判什么标准" ← 主题词就是标准；avatar 期望解剖结构、icon 期望几何精准、装饰类期望剪影丰富

**根因解释**：本铁律解决"用参考图判形态审美"这一具体陷阱。参考图可能本身造型就很简（普通图标），生成图也很简看似一致，但用户的"形态期望"完全独立于参考图（用户期望的是「这个主题在 dribbble 上的合格作品」造型力）。Step C++ 必须切换到"假装我没看过参考图、只知道主题词、判这张图的主体形态够不够格"视角。
```

**Step 2：Commit**

```bash
cd /Users/iflytek/.claude/skills/按图生-评审
git add REVIEW-DISCIPLINE.md
git commit -m "$(cat <<'EOF'
V5.0(WIP): REVIEW-DISCIPLINE.md 新增铁律 5（形态评判禁参照参考图）

- Step C++ 形态审美评判时必须切换视角
- 锚点只有主题词领域先验，参考图退场
- 解决"用参考图判形态"陷阱

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

Expected: commit 成功

---

## Task 5：手动场景化验证（用蓝绿头像案例复测）

**Files:**
- 无文件改动；纯执行验证

**目的**：确保改完的 skill 在真实场景下行为符合预期。

**Step 1：把蓝绿头像样图准备到验证目录**

```bash
mkdir -p /tmp/v5-verify
# 把对话里 user 给的 Image #1（绿图）和 Image #3（蓝图）从 image-cache 复制过来
cp /Users/iflytek/.claude/image-cache/12bbdc56-2b4f-474e-8afb-65f1e4fde35a/1.jpeg /tmp/v5-verify/blue-avatar.jpeg
cp /Users/iflytek/.claude/image-cache/12bbdc56-2b4f-474e-8afb-65f1e4fde35a/3.jpeg /tmp/v5-verify/green-avatar.jpeg
ls -la /tmp/v5-verify/
```

Expected: 两张图都存在

**Step 2：手动调用评审 skill 验证形态评判（蓝图）**

在新对话窗口里手动 invoke 评审 skill：

```
评审请求：
- ref_path: /tmp/v5-verify/blue-avatar.jpeg  （假设这张作参考）
- image_paths:
  1. /tmp/v5-verify/blue-avatar.jpeg
- sampled_index: 0
- theme: 默认头像
- round: 1
```

Expected：评审 JSON 输出包含 `形态评判.评级: "合格"` + 部件枚举至少 3 项（头/颈/肩 或 头/颈/肩/耳）

**Step 3：手动调用评审 skill 验证形态评判（绿图）**

```
评审请求：
- ref_path: /tmp/v5-verify/blue-avatar.jpeg  （仍用蓝图当参考，避免风格不一致干扰）
- image_paths:
  1. /tmp/v5-verify/green-avatar.jpeg
- sampled_index: 0
- theme: 默认头像
- round: 1
```

Expected：
- 评审 JSON 输出包含 `形态评判.评级: "不合格"`
- 部件枚举只有 2 项（头 + 肩）
- `结论: "重生成"`（不管 Step C 评出来是什么，C++ 不合格直接覆盖）
- `结论详情.关键差异前三` 第一条写形态相关问题

**Step 4：根据验证结果决定下一步**

- 如果两次验证都符合预期 → 进入 Task 6 打 v5.0 tag
- 如果蓝图判错（不合格）→ 调 Step C++ 判定参考的「合格门槛」描述
- 如果绿图判错（合格）→ 在 Step C++ judgment prompt 里加更明确的"基础几何元拼接 = 不合格"指导

---

## Task 6：双 repo 打 v5.0 tag + push

**Files:**
- 无文件改动；纯 git 操作

**Step 1：按图生 repo 打 v5.0 tag**

```bash
cd /Users/iflytek/.claude/skills/按图生
git log --oneline -3   # 应该能看到 Task 1 和 Task 2 的两个 V5.0(WIP) commit
git tag v5.0
git push github-personal main
git push github-personal v5.0
```

Expected：main + tag 都 push 成功

**Step 2：按图生-评审 repo 打 v5.0 tag**

```bash
cd /Users/iflytek/.claude/skills/按图生-评审
git log --oneline -3   # 应该能看到 Task 3 和 Task 4 的两个 V5.0(WIP) commit
git tag v5.0
git push github-personal main
git push github-personal v5.0
```

Expected：main + tag 都 push 成功

**Step 3：验证两 tag 都已远程可见**

```bash
cd /Users/iflytek/.claude/skills/按图生 && git ls-remote --tags github-personal | grep v5.0
cd /Users/iflytek/.claude/skills/按图生-评审 && git ls-remote --tags github-personal | grep v5.0
```

Expected：两 repo 都输出 `refs/tags/v5.0`

---

## Task 7：更新 memory 标记 V5.0 为当前基线

**Files:**
- Modify: `/Users/iflytek/.claude/projects/-Users-iflytek/memory/project_anhtu_v3_status.md`

**Step 1：完整重写 memory 文件**

```markdown
---
name: 按图生 skill V5.0 状态（当前基线）
description: 按图生 + 按图生-评审 当前线上版本 V5.0「全自动自迭代 + AI 审美闭环」，按图生 <commit> / 评审 <commit>。仓库独立，分别用 github-personal SSH 别名推送。
type: project
---
# 按图生 skill 演进状态

**当前版本**：V5.0（按图生 <run `git rev-parse HEAD` 取值> / 按图生-评审 <run `git rev-parse HEAD` 取值>）
**当前线上 tag**：`v5.0`（两个 repo 都有）
**前一稳定基线**：`v4.0.4.4`（双线生成迭代，picker 浮窗 + 三轮断点汇报）
**V5 前置安全 tag**：`pre-v5-2026-06-18`（双 repo 都有，回滚锚点）
**仓库**（独立）：
- 按图生：git@github-personal:FengranZhou/claude-skill-antusheng.git
- 按图生-评审：git@github-personal:FengranZhou/claude-skill-antusheng-pingshen.git

**Why**：V5.0 升级动机——用户痛点 3 件套（picker 打扰 / 审美差 / 三轮汇报多余），全部转成全自动 AI 自迭代。

**How to apply**：
- 用户说"看看当前是哪版"→ 用 `git rev-parse HEAD` 对照远程 tag 列表
- 用户说"试错回滚"→ 改前先打里程碑 tag，改完再打新版本 tag
- 不要再相信 memory 里的版本快照，**永远以 git 当前 HEAD + 远程 tags 为准**

## V5.0 关键变更（vs V4.0.4.4）

1. **picker 浮窗废弃** — 主流程同回合并行 Read 3 张图 + 宏观综合挑 winner，强制输出"两张观察 + winner + 一句话原因"
2. **评审加 Step C++ 形态审美评判** — 不参照参考图，仅靠主题词领域先验对主体形态独立打分；不合格直接覆盖为「重生成」
3. **三轮断点汇报废弃** — 改为收敛性检测（连续 3 轮结论 + 关键差异前三 80% 相似才汇报）
4. **doubao-gen.mjs handoff 简化** — `next_action` 永远是 `main_pick_winner` 或 `await_user`，移除 `invoke_review` / `user_picked_neither` 分支
5. **bin/picker 二进制保留不删** — 回滚预案

## 演进脉络（tag 列表）

按时间顺序（两 repo 同步演进）：
- `milestone-2026-06-12` / `pin-2026-06-12` — V3.2 稳定基线
- `v3.3` — 小 bug 修复
- `v3.4` — 提示词 JSON 化
- `v4.0` / `v4.1` / `v4.2` — 双 tab + picker 实验（V4.2 一度被回退到 V3.4，后又通过 V4.0.4.x 系列重新引入并稳定）
- `v4.0.4` ~ `v4.0.4.4` — 双线生成迭代 + picker 浮窗稳定版
- `pre-v5-2026-06-18` — V5 前置安全 tag
- `v5.0` — 全自动自迭代 + AI 审美闭环（当前 HEAD）

## V3 调研未覆盖的 P1/P2 盲区（先放着，真踩到再补）

来自 2026-06-12 调研：
- **VIEWPOINT**（视角/透视）— Midjourney 独立 slot
- **PALETTE 4 元组**（+饱和度/对比度/色温）— style transfer "颜色 DNA"
- **STROKE-PROFILE**（描边粒度，仅 LINE-OUTLINE 时填）
- **装饰元素 POSITION 子字段** — 跨主题位置语义校验
- **SUBJECT-GEOMETRY 描述铁律** — V3.1 候选未实施

用户说"再加新字段"或"按图生还是不准"时，先看是不是已知候选盲区之一。

## 回退教训

V3.2 → V4.x 一路加复杂度（并行/双签/凭证/下载防污染），追"再快一点+再可靠一点"，但用户实际场景下质量已够、速度勉强可忍，附加 ceremony cost 大于收益。V5.0 反过来——减负为主（去 picker / 去汇报），只加 1 个真正缺失的能力（形态评判）。
教训：skill 演进要分清"加能力"vs"加 ceremony"，前者值得后者警惕。
```

**Step 2：写入 commit hash**

```bash
ANTUSHENG_HEAD=$(cd /Users/iflytek/.claude/skills/按图生 && git rev-parse --short HEAD)
PINGSHEN_HEAD=$(cd /Users/iflytek/.claude/skills/按图生-评审 && git rev-parse --short HEAD)
echo "按图生 HEAD: $ANTUSHENG_HEAD"
echo "评审 HEAD: $PINGSHEN_HEAD"
```

把这两个 hash 替换进 memory 文件的 `<commit>` 和 `<run ... 取值>` 占位符。

**Step 3：新增 feedback memory（形态评判铁律）**

新建 `/Users/iflytek/.claude/projects/-Users-iflytek/memory/feedback_anhtu_morphology_review.md`：

```markdown
---
name: 按图生 V5.0 形态评判核心铁律
description: 评审 Step C++ 形态审美评判必须遵守 3 条铁律——禁参照参考图、必须 enumerate 部件、必须给具体原因
type: feedback
---

V5.0 评审 Step C++ 形态审美评判时必须遵守：

1. **禁止参照参考图**——参考图只锚风格，不锚造型力；用参考图判形态会让审美评判退化（参考图本身造型简，生成图造型也简就被判通过，但用户其实嫌丑）
2. **必须先 enumerate 部件再评级**——禁止直接打分（"看着行/不行"是印象，不是评判）
3. **必须给具体原因落到部件/转折/剪欐**——禁止"整体差""不够好"这类虚词

**Why**：2026-06-18 brainstorming 时讨论过 3 种锚点（用户/AI 自己/外部标杆/参考图），用户否决"参考图当锚"和"外部标杆"，确认 AI 凭主题词先验+两张对比的实验可行性，最终落地为 Step C++ 独立硬关卡。

**How to apply**：
- 改评审 skill 时，如果有人提议"加个 X 维度判断"，先问"这个维度的锚点在哪里"——如果答案是"参考图"且该维度独立于风格，**驳回**
- 评审写 prompt 时，必须含主题词作锚 + 两段对比 / 单图都强制原因——这 3 个要素是实验验证可行的关键模式
```

**Step 4：不需要 commit memory**（memory 不在 git repo 里）

Expected：两个 memory 文件都写入并能 Read 出来

---

## 验收总 checklist

实施完成后逐项打勾：

- [ ] Task 0：两 repo `pre-v5-2026-06-18` tag 已 push
- [ ] Task 1：doubao-gen.mjs `node --check` 通过 + 不带参数运行报 Usage
- [ ] Task 2：按图生 SKILL.md Step 3.7 已是「主流程宏观挑 winner」+ Step 4 已是「收敛性兜底」
- [ ] Task 3：评审 SKILL.md 含 Step C++ 段落 + 出参 JSON 含 `形态评判` 字段
- [ ] Task 4：REVIEW-DISCIPLINE.md 含「铁律 5」
- [ ] Task 5：蓝绿头像复测——蓝合格、绿不合格且结论=重生成
- [ ] Task 6：两 repo `v5.0` tag 已 push
- [ ] Task 7：memory project + feedback 文件已更新

## 回滚预案

如 V5.0 上线后行为不符合预期：

```bash
cd /Users/iflytek/.claude/skills/按图生
git checkout pre-v5-2026-06-18

cd /Users/iflytek/.claude/skills/按图生-评审
git checkout pre-v5-2026-06-18

# memory 文件手动恢复 V4.0.4.4 描述（参考 git log of memory commits）
```
